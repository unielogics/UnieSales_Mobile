# Base URL for all API calls. The mobile app is a pure API client.
# NOTE: api.uniesales.com's nginx already prepends /api to the upstream, so the
# base must NOT include /api (otherwise requests double-prefix to /api/api/... → 404).
VITE_API_BASE=https://api.uniesales.com
